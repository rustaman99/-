import java.util.Arrays;
import java.util.Comparator;
import java.util.List;
import java.util.Map;
import java.util.function.Consumer;
import java.util.function.Function;
import java.util.function.Predicate;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class sor {
    public static void main(String[] args) {
//            List<String> names = Arrays.asList("Smith", "Ruslan", "Catley", "Sie");
//            Predicate<String> nameStartsWithS = str -> str.startsWith("S");
//            names.stream().filter(nameStartsWithS).forEach(System.out::println);

//        System.out.println();
//        Predicate<Integer> predicate= x -> x > 5;
//        System.out.println(predicate.test(6));
//
//        List<String> name=Arrays.asList("Ruslan","Dana","Ben","Den");
//        Predicate<String> nameStartsD=s -> s.startsWith("R");
//        Predicate<String> length = s->s.length() >=4;
//        name.stream()
//                .filter(nameStartsD.and(length))
//                .forEach(s -> System.out.println(s));

//        Stream.of(120, 410, 85, 32, 314, 12)
//                .sorted(Comparator.reverseOrder())
//                .forEach(System.out::println);
//               List<String> names=Arrays.asList("Ruslan","Ivan","Stepan","Dan","Ruslan","Ivan","Stepan","Dan","Ivan","Stepan","Dan","Ivan");
//        Map<String,Long> repetitionAmount=
//                names.stream().collect(Collectors.groupingBy(Function.identity(),Collectors.counting()));
//        repetitionAmount.forEach((k,v)-> System.out.println(k+ " : " + v ));
//        Consumer<String> printConsumer = t -> System.out.println(t);
//        Stream<String> cities = Stream.of("Sydney", "Dhaka", "New York", "London");
//        cities.forEach(printConsumer);

        }

}
